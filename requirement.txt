pip install numpy
pip3 install scikit-learn
pip3 install ryu
pip install --upgrade pip
pip install git+https://github.com/osrg/ryu.git
python -m venv ryu_venv
source ryu_venv/bin/activate
pip install --upgrade setuptools
